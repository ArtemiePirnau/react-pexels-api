import React, { Component, Fragment } from "react";
import ImagesList from "../images-list/images-list";

const CodingList = ({ type }) => {
  return <ImagesList type={type} />;
};

export default CodingList;
